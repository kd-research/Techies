[tool.poetry]
package-mode = false

[tool.poetry.dependencies]
python = "3.12.4"
crewai = {extras = ["tool"], version = "^0.36.0"}
langchain-community = "^0.2.6"
langchain-huggingface = "^0.0.3"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
